name: excel_viewer_pro
description: تطبيق عربي احترافي لفتح وعرض ملفات Excel بصيغة XLSX داخل التطبيق.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: 10.3.8
  excel: ^4.0.6
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
